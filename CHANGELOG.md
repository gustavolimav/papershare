# Changelog

All notable changes to Papershare are documented here.

Format: [Keep a Changelog](https://keepachangelog.com/en/1.0.0/).
Versioning: [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

---

## [Unreleased]

### Added

- Phase 11 (Visual Identity & UI Redesign) — US-49, Superadmin as a Settings subsection: `components/settings/SettingsTabs.tsx` gains two more (optional) tabs, "Migrations" and "Feature flags", rendered only when `app/(app)/settings/page.tsx` passes them (i.e. only for `user.is_superadmin`) — a non-superadmin's tab list is unchanged at five. Removed the separate "Superadmin" sidebar item from `components/layout/AppShell.tsx` and deleted the now-unused `app/(app)/superadmin/migrations` and `app/(app)/superadmin/feature-flags` pages; both tools now sit as sibling tabs inside Configurações instead of a second, parallel nav entry only some users saw. `components/superadmin/MigrationsPanel.tsx`/`FeatureFlagsPanel.tsx` reused unchanged. Updated the three code comments that referenced the old `/superadmin/*` paths (`infra/auth.ts`, `components/billing/BillingSettingsForm.tsx`, two billing test files) to point at the Feature flags tab instead.
- Post-Phase-11 audit fix — analytics page-attention heatmap now renders at the document level, not just inside the per-link drawer: `models/linkView.ts#getAnalyticsByDocumentId` already had a page-breakdown aggregation helper (`getPageBreakdownByDocumentId`, previously only consumed internally by the AI drop-off-insight feature) sitting unused for this purpose — wired it into the response (`DocumentAnalyticsResponse.page_breakdown`, additive field) and `components/analytics/AnalyticsView.tsx` now renders the existing `PageHeatmapChart` component inline, right after the stat cards, matching the design prototype's layout (the prior page only showed a per-day line chart and a links table, with the actual page-by-page heatmap hidden until you opened a specific link's drawer).
- Post-Phase-11 audit fix — dashboard document list gains a "Buscar documentos..." search input (`components/documents/DocumentList.tsx`), matching the prototype's header row; filters the current page's documents by title, client-side (a full server-side search isn't worth the API surface yet at current per-workspace document counts). Follow-up: moved the search box and the "Meus documentos" heading into one shared header row (new `components/dashboard/DashboardContent.tsx`, replacing the old split between a server-rendered `<h1>` in `app/(app)/dashboard/page.tsx` and `DocumentList`'s own internal search state) so the two sit side by side like the prototype instead of stacked.
- Sidebar nav gains three more destinations — Atividade, Links, Contatos — shipped as frontend-only mocks ahead of their real Phase 12/13/14 backend work: `components/activity/ActivityFeed.tsx` (day-grouped event list), `components/links-inventory/LinksInventory.tsx` (cross-document link table with a working copy-link button), and `components/contacts/ContactsList.tsx` (cross-document viewer directory, "Gerar follow-up" currently a toast pointing at Phase 14). Each is static, hardcoded sample data matching the design prototype's copy/layout — no new API routes, tables, or models. See TODO.md's Phase 12/13/14 sections for what's left to make them real.
- Phase 12 (Activity Feed) — US-52, real backend for `/activity`: new `GET /api/v1/activity` (`models/activity.ts`), a `UNION ALL` of `link_views` and `share_links` scoped to `request.user.active_workspace_id` (same trust boundary as `GET /api/v1/documents` — no separate `requireRole` call), ordered `created_at desc` and paginated. Covers view events (with a self-join detecting same-fingerprint revisits) and link-creation events; NDA-acceptance and blocked-download events are deferred since neither is persisted anywhere today (tracked as follow-up items in TODO.md's Phase 12 section). `components/activity/ActivityFeed.tsx` rewritten to fetch this endpoint via `useSWR` instead of its earlier static mock, grouping client-side into Hoje/Ontem/Esta semana/Mais antigo. New integration tests in `tests/integration/api/v1/activity/get.test.ts` cover actor/detail formatting for both event types, revisit detection, cross-workspace isolation, and pagination.
- Phase 11 (Visual Identity & UI Redesign) — US-48, document detail responsive fix: `components/documents/DocumentMeta.tsx`'s header row and `components/share-links/ShareLinkCard.tsx`'s Editar/Revogar/Duplicar row both gain `flex-wrap`, so at narrow viewport widths (~530–700px) the action buttons wrap onto their own line instead of clipping off-screen. Pure CSS — no behavior change, desktop-width layout unaffected.
- Phase 11 (Visual Identity & UI Redesign) — US-50, auth tabs: new `components/forms/AuthTabs.tsx`, a segmented "Entrar / Criar conta" pill switcher rendered above the card header on both `/login` and `/register`, matching the design prototype's single tabbed auth card. Each tab is a real `next/link` to the other route — not a client-side swap — so both URLs, deep links, and the homepage's `/register` links all keep working exactly as before. Removed the now-redundant "Não tem uma conta?"/"Já tem uma conta?" text links from the bottom of `LoginForm.tsx`/`RegisterForm.tsx` since the tab switcher does the same job.
- Phase 11 (Visual Identity & UI Redesign) — US-51, document detail tabs: new `components/documents/DocumentTabs.tsx`, a "Visão geral / Análises" tab bar (active tab underlined) rendered on both `/documents/[id]` and `/documents/[id]/analytics`, matching the design prototype's single tabbed document screen. Real navigation between the two existing routes, not a route merge — `tests/e2e/engagement-score.spec.ts` (which navigates directly to the analytics URL) still passes unchanged. The analytics page's breadcrumb nav is replaced by the tab bar, and its heading now reads "{título} — Analytics" instead of a bare "Analytics" so the document identity doesn't disappear now that the breadcrumb is gone. Removed the standalone "Analytics" button from `DocumentMeta.tsx` now that the tab bar does the same job.

### Fixed

- `ServiceError`'s constructor accepted an `action` field in its options type but silently ignored it, always hardcoding the same generic "Verifique se o serviço está disponível." — so the specific, actionable message the new BYOK follow-up-email feature needs ("Peça para o dono do documento configurar sua chave de IA em Configurações.") never actually reached the response. Caught via manual verification in-browser. Every other custom error class in `infra/errors.ts` already respected a passed-in `action`; `ServiceError` was the one exception. Existing `ServiceError` call sites (`infra/storage.ts`, `infra/database.ts`) don't pass `action` explicitly, so they keep the same generic fallback message as before.
- `ServiceError` (503) instances thrown anywhere in the app — including the new AI-unavailable case below, but also pre-existing ones in `infra/storage.ts` and `infra/database.ts` — fell through `infra/controller.ts#onErrorHandler`'s `instanceof` chain (which checked every other custom error class except this one) into the generic `InternalServerError` fallback. The response's status code happened to still come out as 503 (the fallback forwards `err.statusCode`), but the specific, actionable `message`/`action` were replaced with the generic "Um erro interno não esperado aconteceu." — masked in the browser as an unhelpful error. Caught via manual verification of the new follow-up-email feature's "AI unavailable" message. Added the missing `err instanceof ServiceError` branch.
- Custom branding's accent color (`brand_accent_color`) correctly overrode the `--primary` CSS variable on the ready-state wrapper, but no element in the actual PDF viewing experience used `bg-primary`/`text-primary` — every toolbar button in `components/viewer/ViewerControls.tsx` used the `ghost`/`outline` variants, so the color was wired up but never visible for the common case (viewing a PDF). Changed the toolbar's "Download" button to the `default` variant (`bg-primary`), so it now visibly picks up the link's accent color when set, and falls back to the normal theme color otherwise (verified no visual change for links without branding configured).
- Vercel build warned `Node.js version 20.x is deprecated. Deployments created on or after 2026-10-01 will fail to build.` Bumped `package.json#engines`, `.nvmrc`, and both GitHub Actions workflows from Node 20 (`lts/iron`) to `24.x`, matching Vercel's own suggested fix, so local dev, CI, and Vercel all build on the same, still-supported major.
- `/documents/[id]/analytics` (the per-link/per-page analytics dashboard, including the page-view heatmap) was fully built and reachable by direct URL, but nothing in the UI linked to it — the document detail page had no way to navigate there. Added an "Analytics" button next to "Editar" in `components/documents/DocumentMeta.tsx`.
- Vercel build warned that `package.json`'s `"engines": { "node": ">=20.16.0" }` is open-ended, so it overrides the Project Settings dropdown ("22.x") and silently jumps to whatever the newest major Node release is (24.x at the time) instead — meaning a future Node major release could change the runtime under this app with no code change and no warning beyond a build log line. Bounded the range to `>=20.16.0 <21.0.0`, matching `.nvmrc` (`lts/iron`) and the GitHub Actions workflows, so the version actually used is explicit and stable across local dev, CI, and Vercel.

- CI (`Jest Ubuntu`) was failing on every document-upload-dependent test with "Unexpected token < in JSON": `@aws-sdk/client-s3` requires Node `>=20.0.0` and `pdf-parse`/`pdfjs-dist` require Node `>=20.16.0`, but CI and `.nvmrc` were pinned to `lts/hydrogen` (Node 18) — confirmed via `npm warn EBADENGINE` in the CI log. Bumped `.nvmrc` and both GitHub Actions workflows to `lts/iron` (Node 20), and added an `engines` field to `package.json` so this fails fast with a clear message instead of an obscure runtime crash next time.
- Public PDF viewer (`components/viewer/PDFViewer.tsx`) threw `TypeError: Object.defineProperty called on non-object` on every load, from inside `pdfjs-dist`'s own bundled module code. Confirmed via a native `<script type="module">` test that the unmodified `pdf.mjs` works fine in the same browser — the crash only happens when Next's webpack re-bundles the package's already-webpack-bundled `.mjs`, corrupting its internal export wiring. Fixed by loading both the library and its worker as real native ES modules straight from the jsdelivr CDN (`import(/* webpackIgnore: true */ ...)`), instead of importing the npm package.
- Vercel's production build (`next build`) failed with 26 real ESLint errors across the new frontend files (`'React' is not defined` / `no-undef`, `'x' is defined but never used` / `no-unused-vars`) that never showed up locally in `npm run sf`. Root cause: `.eslintrc.json` was missing `"root": true`, so ESLint's legacy config resolution walked up past the project into an ancestor directory, found another copy of the same plugin, and hard-aborted linting entirely — silently no-op'ing every local lint run without failing the command. Separately, `eslint:recommended`'s base `no-undef`/`no-unused-vars` (extended after `next/core-web-vitals`) were clobbering Next's TypeScript-aware versions of those rules, which don't understand type-only positions (e.g. a named parameter in a function _type_ signature, or a bare `React.FormEvent` type reference) and flag them as real undefined/unused variables. Fixed by adding `"root": true` and an override enabling `@typescript-eslint/no-unused-vars` (and disabling the base rules) for `*.ts`/`*.tsx` files — the standard fix recommended by typescript-eslint for this exact class of false positive.
- `GET /api/v1/documents` (dashboard document listing) 500'd on every request on Vercel with `ReferenceError: DOMMatrix is not defined`, thrown from inside `pdfjs-dist`'s legacy Node build. Cause: `pdf-parse` was imported at the top of `pages/api/v1/documents/index.ts`, a single file that backs both the `GET` (list) and `POST` (upload) handlers as one serverless function — so evaluating that top-level import ran on every request regardless of method, and `pdfjs-dist`'s legacy build references browser canvas globals (`DOMMatrix`) that only exist if a native canvas binary (`@napi-rs/canvas`) loads successfully, which apparently didn't happen on Vercel's Linux runtime even though it worked locally on macOS. Fixed by lazily `import()`-ing `pdf-parse` only inside `extractPageCount()`, so listing/deleting/etc. documents never touches it.
  - That alone wasn't enough: actual PDF uploads still 500'd, because `pdfjs-dist`'s internal geometry code throws the same `DOMMatrix` error in a way that isn't reliably caught by a try/catch around the awaited call (not tied to the promise `getInfo()` returns — looks like an internal fire-and-forget operation). Fixed for real by probing `@napi-rs/canvas` with its own `import()` first — a plain, catchable module-resolution failure — and skipping page-count extraction entirely (falling back to `page_count: null`) when that probe fails, instead of ever calling into `pdf-parse` without it.
- Downloading a password-protected share link always returned `403 Senha incorreta`, even right after typing the correct password to view it. Cause: the download button (`components/viewer/PDFViewer.tsx`) and the non-PDF fallback link (`components/viewer/ViewerPage.tsx`) both pointed the browser straight at `/api/v1/share/[token]/file` via `window.open`/`<a href>` — a plain navigation can't attach the `X-Share-Password` header the route requires, so it always looked like no password was supplied. Fixed by downloading through `fetch()` with the header instead: PDFs reuse the bytes already fetched for the viewer (no extra request), other file types fetch fresh with the remembered password, and either way the bytes are handed to the browser as a `Blob` object URL via a programmatically-clicked anchor.

### Added

- Phase 11 (Visual Identity & UI Redesign) — US-45, analytics dashboard restyle: `app/(app)/documents/[id]/analytics/page.tsx` and everything `components/analytics/AnalyticsView.tsx` composes now read the US-39 tokens/typography — pure styling, no change to data fetching, prop shapes, or the existing Free/Pro engagement-score gating (US-36). `StatCard` values render in the new serif heading font with an uppercase muted label. `PageHeatmapChart`'s bars stay a single hue instead of switching color by value: each `<Bar>` renders its own `<Cell>` with `fillOpacity` scaled by that page's attention relative to the busiest page (full-strength `--primary` at the max, down to a 25%-opacity tint at the low end), so the chart still reads as one continuous gradient of the same accent. `InsightCard` becomes a soft `--primary`-tinted callout (`border-primary/20 bg-primary/5`) with a `Sparkles` icon and a bold "Insight da IA:" lead-in inline with the existing `models/analyticsInsights.ts` copy — same BYOK-gated show/hide as before. `ViewerEngagementList` rows gain an initials avatar circle (same `bg-primary/15`/`text-primary` treatment as the sidebar's own user avatar) and their score badge now uses the new `--score-good`/`--score-warn`/`--score-critical` tokens (soft-tinted, same 70/40 thresholds as before) instead of the generic default/secondary/outline `Badge` variants; the per-viewer suggestion button is relabeled "Gerar follow-up" (previously "Sugerir e-mail") to match the prototype's copy. `AnalyticsView`'s "Visualizações por dia"/"Top links" sections are wrapped in `Card`s for visual consistency with the rest of the page.
- Phase 11 (Visual Identity & UI Redesign) — US-43, dashboard restyle: `DocumentList`/`DocumentCard` become a data table (Nome/Visualizações/Links/Pontuação/Atualizado, `DocumentCard.tsx` now exports a `DocumentRow`) with a file-type icon chip (PDF/DOC/PPT) and a color-coded score badge using the US-39 `--score-good`/`--score-warn`/`--score-critical` tokens (≥80 good, 50-79 warn, <50 critical); new `components/ui/table.tsx` (shadcn-style, no new dependency). `app/(app)/dashboard/page.tsx` gains a 4-stat-card row (Documentos, Links ativos, Engajamento médio, Visualizações totais) above the table via a new `components/dashboard/DashboardStats.tsx` — raw numbers only, no trend deltas, since a real period-over-period comparison needs historical snapshots this phase doesn't have. The one backend change in this otherwise pure-restyle phase: `GET /api/v1/documents` (`models/document.ts#findAllByWorkspaceId`, `DocumentListItem` in `types/index.ts`) gains three computed, additive-only fields per document — `view_count`/`active_link_count` (same per-document subquery pattern `WorkspaceWithRole` already uses for `document_count`/`active_link_count`, US-36) and `engagement_score`, a simpler proxy (average `time_on_page` across the document's views, normalized against the same 120s target `models/linkView.ts#computeEngagementScore` uses, capped at 100) rather than a full per-viewer aggregate, since the real formula needs fingerprint-grouping that doesn't reduce to one document-level subquery. Caught during implementation: Postgres's `LEAST`/`GREATEST` ignore `NULL` arguments instead of propagating them (unlike standard `MIN`/`MAX`), so a naive `COALESCE(LEAST(100, ...), 0)` silently returned `100` for documents with zero views instead of `0` — fixed with an explicit `CASE WHEN AVG(...) IS NULL THEN 0 ELSE ...`, caught by a new test asserting the all-zero case.
- Phase 11 (Visual Identity & UI Redesign) — US-41, homepage restyle: `app/page.tsx`'s hero now matches the prototype — an uppercase pill badge ("Compartilhamento de documentos, feito para gerar confiança"), a two-line serif headline ("Envie. Veja o que acontece depois."), and a primary/secondary CTA pair ("Começar grátis" / "Ver um visualizador ao vivo", same `/register`/`/login` destinations as before). The four US-38 feature sections and the Free/Pro/Business pricing table keep their exact copy/data but pick up the new warm-cream/terracotta tokens, serif section headings, icon chips on each feature card, and a "Mais popular" badge on the Pro pricing card (new `components/ui/badge.tsx` usage, no new component). `components/layout/Header.tsx` and `Footer.tsx` (the public/pre-auth variant every other public page shares) restyled to match — `Header` also gained the `ThemeToggle` (built in US-39, not wired into any page until now) so light/dark mode is reachable pre-login too, not just from the authenticated sidebar. Styling only: no copy, link destination, or data changed.
- Phase 11 (Visual Identity & UI Redesign) — US-42, auth pages restyle: `/login`, `/register`, `/forgot-password`, `/reset-password/[token]` all get the same centered-card treatment now — a "Papershare" wordmark link (back to `/`) above the card, a serif `CardTitle` heading matching the prototype's copy ("Bem-vindo de volta" for login, "Crie sua conta" for register), and taller (`h-10`) inputs/`size="lg"` submit buttons for a more deliberate auth-form feel. Pure restyle: `LoginForm`/`RegisterForm`/`ForgotPasswordForm`/`ResetPasswordForm` keep the same field names, validation, and submit handlers — only className changes. No Google OAuth button added, per the design doc's Key Decisions (real OAuth is out of scope for this phase). `Header.tsx`/`Footer.tsx` untouched, left for US-41 to restyle.
- Phase 11 (Visual Identity & UI Redesign) — US-44, document detail & share-link manager restyle: `app/(app)/documents/[id]/page.tsx`, `DocumentDetailView.tsx`, `DocumentMeta.tsx`, `EditDocumentForm.tsx`, `SummaryCard.tsx`, and the share-link manager (`ShareLinkList.tsx`, `ShareLinkCard.tsx`, `CreateShareLinkModal.tsx`, `EditShareLinkModal.tsx`) now use the US-39 tokens/typography — audited all eight files for hardcoded gray/neutral Tailwind classes bypassing the token system and found none (they already read `bg-muted`/`text-muted-foreground`/`text-destructive`, so the new warm cream/terracotta palette applies automatically), and every shadcn `CardTitle`/`DialogTitle`/`AlertDialogTitle` in this flow already picks up serif headings for free from US-39's `font-heading` wiring. The two raw headings this flow renders outside those primitives — the document title `<h1>` in `DocumentMeta.tsx` and the "Links de compartilhamento" `<h2>` in `ShareLinkList.tsx` — gained the `font-heading` utility so they render serif too. No change to props, data fetching, mutations, or the Free-plan gating logic (US-36/37) on the share-link fields.
- Phase 11 (Visual Identity & UI Redesign) — US-46, settings restyle (tab navigation): `app/(app)/settings/page.tsx`'s five `<Separator>`-divided stacked sections (Perfil, Chave de IA, Equipe, Faturamento, Zona de perigo) become a left-side tab list + content panel, matching the prototype instead of requiring a long scroll to reach the last section. New `components/settings/SettingsTabs.tsx` is a small client component holding just the active-tab `useState` — deliberately not shadcn's `Tabs` primitive, since Radix's `Tabs.Content` unmounts inactive panels by default and forcing it to stay mounted (`forceMount` + manual styling) would've been more code than a plain conditional-class toggle for this vertical, sidebar-style tab list. Every panel stays mounted at all times (visibility toggled via `hidden`/`block`, never conditional rendering), so switching tabs can't discard an in-progress edit in another one, same as today's always-scrolled-into-view behavior. "Zona de perigo" keeps a destructive-toned (`text-destructive`) tab label. No prop or behavior changes to `ProfileForm`, `AiSettingsForm`, `TeamSettingsForm`, `BillingSettingsForm`, or `DangerZone` — this is a pure layout restructure of the page around them.
- Phase 11 (Visual Identity & UI Redesign) — US-40, app shell (sidebar navigation): every authenticated page (`/dashboard`, `/documents/[id]`, `/documents/[id]/analytics`, `/settings`, `/superadmin/*`) moves into a new `app/(app)/` route group (path-transparent — URLs unchanged) wrapped by a new `components/layout/AppShell.tsx`: a persistent left sidebar with the workspace switcher (moved here from `Header.tsx`), Documentos/Configurações nav (plus a conditional Superadmin item), and a footer with the user's avatar/name/plan and the US-39 `ThemeToggle`. Sidebar nav intentionally ships with only two real destinations — Atividade/Links/Contatos are added by Phases 12-14 respectively, not built as placeholders. `components/layout/Header.tsx` now only ever renders the public/pre-auth nav (Entrar/Cadastrar), since every authenticated page uses the sidebar instead. Public/pre-auth pages (`/`, `/login`, `/register`, `/forgot-password`, `/reset-password/[token]`, `/em-breve`) are unaffected. Full `npm run test:e2e` suite re-run against this change (27/27 passing) given the header→sidebar move was the one change in this phase expected to risk breaking an existing Playwright locator — it didn't.
- Phase 11 (Visual Identity & UI Redesign) — US-47, public viewer & error states restyle: `components/viewer/ViewerPage.tsx`, `PDFViewer.tsx`, and `ViewerControls.tsx` pick up the US-39 tokens (toolbar grouped into pill segments on `bg-card`, rounded canvas, warm background) with no change to PDF rendering, download logic, or page-tracking. `PasswordGate.tsx`, `EmailGate.tsx`, and `NdaGate.tsx` are rebuilt around the auth-page card treatment (centered `Card`, icon circle, serif `CardTitle`, styled inputs) with identical validation/submit behavior. Every error/empty state reachable from a public share link — link not found, revoked, expired, the generic fallback, a corrupted-PDF load failure, and the "preview not available for this file type" case — now renders through a new shared `ViewerStateCard` (icon circle, serif heading, muted description, single CTA back to `/`) inside a new `ViewerCardShell` layout; `ViewerPage`'s `LoadState` gained a presentation-only `kind` tag (`not-found`/`revoked`/`expired`/`generic`) derived from the same server messages already returned today, so the description text shown is unchanged. New, not a restyle: a small "Desenvolvido com Papershare" attribution footer (`components/viewer/ViewerFooter.tsx`), linking to the marketing homepage, now appears at the bottom of every public viewer page and every gate — light product-attribution branding for the document recipients (usually not Papershare customers themselves) who are the first people to see these pages.
- Phase 11 (Visual Identity & UI Redesign) — US-39, design tokens & typography: `app/globals.css`'s neutral shadcn palette is replaced with a warm cream + terracotta identity (`--primary: oklch(0.58 0.16 38)`), with a separately-tuned (not inverted) dark palette using a brighter coral accent to stay legible on a warm near-black ground. Three new semantic tokens (`--score-good`/`--score-warn`/`--score-critical`, light + dark) are added for the score/status badges upcoming restyle stories need. `app/layout.tsx` swaps `Inter` for `Manrope` (body) + `Source_Serif_4` (headings, via a new `--font-serif` variable) — both via `next/font/google`, same mechanism as before. Wired `--font-heading` (already read by `CardTitle`/dialog components, but only inside an unused `.theme` class before this — a silent no-op) directly into the theme so it actually resolves to the new serif font, plus a base-layer rule applying it to raw `h1`/`h2`/`h3` tags site-wide. Finished the previously-half-installed dark mode support: added `next-themes`' `ThemeProvider` to `app/providers.tsx` (it was a dependency already, and `components/ui/sonner.tsx` already called `useTheme()`, but nothing ever rendered the provider or flipped the `.dark` class) and a new `components/layout/ThemeToggle.tsx` — not wired into any page yet, that's the app-shell story's job. See `docs/plans/2026-07-21-visual-identity-design.md` for the full token spec and `user-stories/phase-11-visual-identity/` for the story breakdown (US-39 through US-47).
- Self-service password recovery: `POST /api/v1/password-reset` (rate-limited, always responds `204` whether or not the email is registered — otherwise the endpoint becomes an oracle for checking which emails exist) creates a single-use token in a new `password_reset_tokens` table (migration `031`, same "only store a SHA-256 hash" shape as `sessions`) and e-mails a link via `infra/mailer.ts#sendPasswordResetEmail`; requesting a new link invalidates any previous one (`models/passwordReset.ts#create` deletes existing rows for that user first). `PATCH /api/v1/password-reset/[token]` validates the token (`404` if missing/expired/already used), sets the new password via a new `models/user.ts#resetPassword(userId, newPassword)` (bypasses the current-password check `updateByUsername` would otherwise need, since the token itself is the credential here), then deletes the token and every existing session for that user (`session.deleteByUserId`, same helper US-31's account soft-delete already uses) — a reset password logs out every device, not just the one that requested it. New `/forgot-password` and `/reset-password/[token]` pages/forms follow the existing `LoginForm`/`RegisterForm` conventions; the forgot-password success state shows the same "if this email exists..." message regardless of outcome, matching the backend's no-enumeration behavior. `LoginForm` gained an "Esqueceu sua senha?" link and a success toast when redirected back with `?password-reset=success`.
- Phase 10 (Monetization) — soft-launch safety switch for billing: real Stripe checkout/portal aren't ready to expose to every user yet (live-mode Stripe account, webhook, and Customer Portal all still need configuring — see `TODO.md`), so both are now gated behind a new `billing_stripe` feature flag instead of shipping live to everyone the moment US-35-38 merge. New `feature_flags` table (`key`, `enabled`) follows the same "no row = off" pattern as `subscriptions` (no row = Free) — a flag needs no seed migration to ship. `models/featureFlag.ts#requireEnabled()` throws the same `ServiceError` (503) shape as an unconfigured external service when a flag is off, called from both `.../billing/checkout` and `.../billing/portal` right before the existing Stripe-configured check, so a direct API call is blocked exactly the same way whether Stripe itself or just the flag isn't ready. `BillingSettingsForm`'s "Assinar Pro"/"Assinar Business"/"Gerenciar assinatura" buttons check the flag (`GET /api/v1/feature-flags`, any logged-in user) before ever calling the API — off by default, so they route to a new `/em-breve` page instead of hitting an endpoint that would just 503 anyway, matching this codebase's established "match the UI to what the API allows" philosophy from the plan-gating work. Only a superadmin can flip it: new `PATCH /api/v1/feature-flags/[key]` behind a new `requireSuperadminMiddleware` (same single-status-for-either-failure-reason shape as `migrationsAuthMiddleware`, just without its shared-secret bypass — this is a pure app-level admin action, not something CI needs to do headlessly), with a toggle UI at `/superadmin/feature-flags` alongside the existing Migrations panel.
- Playwright end-to-end suite (`tests/e2e/`) covering plan-gating (US-36/US-37) in a real browser, not just via API status codes: document/active-link limits (Free capped at 10, Pro/Business unlimited, downgrade preserves existing data), the watermark/NDA/allow-list/branding fields rendering disabled with a hint on Free, engagement-score visibility, and the Faturamento tab (plan/usage display, owner-only buttons, non-owner read-only view, the checkout button's 503-toast fallback when Stripe isn't configured, and its `billing_stripe`-flag-off redirect to `/em-breve`). Each spec asserts both the UI (disabled attributes, inline messages, dialog content) and the backend (`402`/`200` status codes, response bodies) for the same scenario, since either layer regressing independently is a real risk this feature area has already hit once (see US-37's `EditShareLinkModal` fix). `tests/e2e/helpers.ts` reuses `tests/orchestrator.ts` directly — Playwright specs run in plain Node, so the same DB-backed fixtures (`activateSubscription`, `uploadDocument`, etc.) work in both suites without duplication. Run with `npm run test:e2e`; `jest.config.js` gained `testPathIgnorePatterns` for `tests/e2e/` so Jest doesn't try to execute Playwright's `.spec.ts` files as its own tests.
  - Surfaced two real fixes along the way: `infra/database.ts` named-imported `{ Client, Pool }` from `pg` (a CJS package) — works under webpack/ts-jest's own CJS interop, but not under Playwright's native-ESM module loading (`SyntaxError: The requested module 'pg' does not provide an export named 'Client'`); changed to destructuring from the default import instead, which is interop-safe everywhere. `components/share-links/ShareLinkList.tsx`'s `<section>` had no accessible name (just an adjacent `<h2>`), so Chromium's implicit "region" role detection for it was inconsistent — added `aria-labelledby` pointing at the heading, a legitimate a11y fix that also made the section reliably targetable by role.
- Phase 10 (Monetization) — US-38, homepage revamp: `app/page.tsx`'s flat 3-card feature grid (written back when only Phase 3-5 existed) is replaced with four themed sections — Documentos & Compartilhamento, Segurança & Confiança, Analytics & IA, Equipe — covering every feature shipped through Phase 9, plus a Free/Pro/Business pricing section with a `/register` CTA on each card (pricing is informational pre-signup only; this page doesn't call the billing API or read auth state — actual plan selection happens in Settings → Faturamento, per US-37). Hero section unchanged.
- Phase 10 (Monetization) — US-37, frontend billing & usage-aware UI: new "Faturamento" tab in `/settings` (`components/billing/BillingSettingsForm.tsx`, same insertion pattern as "IA"/"Equipe") shows the workspace's plan, a Free-tier usage line ("X de 10 documentos · Y de 10 links ativos") reading fields `WorkspaceWithRole` already carries from US-36 (no extra fetch), and owner-only "Assinar Pro"/"Assinar Business"/"Gerenciar assinatura" buttons that redirect to Stripe Checkout/Portal; returning from Checkout with `?checkout=success` revalidates `WORKSPACES_KEY` and shows a toast so the new plan reflects without a manual reload. Rather than let a Free-plan user hit a raw `402`, gating is surfaced proactively wherever it'll actually bite: `DocumentList`/`ShareLinkList` replace the upload zone/"Criar novo link" trigger with an inline upgrade message once the workspace is at its Free-tier limit, and `CreateShareLinkModal`/`EditShareLinkModal` render the watermark/NDA/allow-list/branding fields `disabled` with a "Recurso do plano Pro" hint — disabled only while the field is currently off/empty, so a link that already had one of these set from before a downgrade can still be turned off or cleared (never re-enabled) from the UI, matching the backend's "removal is never gated" rule from US-36. `EditShareLinkModal` also stopped always resending all four gated fields' existing values on every save (previously would have 402'd any edit — even just relabeling — on a Free workspace for a link with one already set); now it only includes a gated field in the `PATCH` body when its value actually changed. A shared `lib/toastPaymentRequired.ts` surfaces any `402` that still slips past the proactive gating (e.g. a stale SWR cache across tabs) as a toast with the error's `message`/`action`, wired into the upload, create, and edit flows as a fallback.
- Phase 10 (Monetization) — US-36, plan gating: Free-plan workspaces are now actually capped instead of just having the plumbing sit unused. `models/document.ts#create()` and `models/shareLink.ts#create()` each gained an `assertWithin*Limit()` check (10 documents, 10 _active_ share links — counted workspace-wide across every document, not per-document) that throws the new `PaymentRequiredError` (402, added in US-35) once the plan resolves to `"free"` via `models/subscription.ts#getPlanForWorkspace()`; revoking a link frees a slot since only `is_active = true` rows count. Watermark, NDA gate, email allow-list, and custom branding move behind Pro/Business: a shared `assertGatedFeaturesAllowed()` in `models/shareLink.ts` checks both `create()` and `updateById()`, but only against fields actually present in the input — `undefined` (not being touched) is never gated, so PATCHing an unrelated field on a link that already has e.g. `watermark_enabled: true` from a since-downgraded Pro period still succeeds, and explicitly turning a feature _off_ is never gated either, only newly enabling one is. Engagement scoring follows the same "existing data survives a downgrade" philosophy but at the read layer instead of write: `GET .../links/[linkId]/analytics` resolves the link's workspace plan and nulls out just the `viewers` field (now typed `ViewerEngagement[] | null`) on Free, leaving every other stat (views, page breakdown) unaffected, rather than 402ing the whole endpoint. `WorkspaceWithRole` (returned by `GET /api/v1/workspaces`) gains `plan`, `document_count`, and `active_link_count` — same subquery pattern as `member_count`/`ai_configured` from US-32/33 — so the frontend billing UI (US-37) can show usage without an extra request. Uncovered while wiring this up: a dozen-plus pre-existing tests created share links with a gated field directly (to test NDA/watermark/branding _behavior_, not plan gating) — each now activates a Pro subscription first via a new `orchestrator.activateSubscription()` test helper (a direct `subscriptions` insert, bypassing Stripe entirely, same spirit as `promoteToSuperadmin`), so they keep testing what they were originally designed to test.
- Phase 10 (Monetization) — US-35, billing infrastructure, scoped per **workspace** rather than per user (documents/links already belong to `workspace_id` since Phase 9, and Business-tier features are inherently team-shared). New `subscriptions` table (migration `029`, `UNIQUE` on `workspace_id`) — a workspace with no row is on the Free plan, so there's no backfill for existing workspaces; `plan` is only ever `'pro'`/`'business'`, never `'free'`. `models/subscription.ts#getPlanForWorkspace()` resolves `'free'` whenever no row exists or its `status` isn't `'active'` (payment failed, canceled), which is also how a downgrade behaves without any extra code — existing documents/links keep working, the workspace just can't use gated features or create new ones past the (not-yet-enforced-until-US-36) Free limits. `infra/stripe.ts` wraps the `stripe` SDK with the same degrade-gracefully pattern as `infra/ai.ts`: `STRIPE_SECRET_KEY` unset (the default in local dev/CI, same as `ANTHROPIC`/`RESEND` keys) makes `POST /api/v1/workspaces/[id]/billing/checkout` and `.../billing/portal` (both owner-only) return `503` instead of crashing. New `PaymentRequiredError` (402) added to `infra/errors.ts`/`infra/controller.ts`, ready for US-36's limit/feature gating. `POST /api/v1/webhooks/stripe` verifies the `stripe-signature` header before touching anything — the one endpoint where a forged request would grant a free paid plan. Deliberate deviation from the original design: the webhook treats `checkout.session.completed` as a no-op rather than the write path, because that event's payload doesn't include the subscription's price or billing period without an extra Stripe API call; `customer.subscription.created`/`.updated` (which Stripe always fires in the same flow) carry the full object inline instead, so `subscription_data.metadata.workspace_id` set at checkout time is all that's needed to resolve everything with zero extra API calls — this also means the webhook's signature-verification and upsert behavior are fully testable locally via `stripe.webhooks.generateTestHeaderString()` against a dummy `STRIPE_WEBHOOK_SECRET`, no real Stripe test-mode account required (see `.env.development`).
- Phase 9 (Team Workspaces, "workspaces básico" slice) — US-33, frontend workspace switcher & team settings, closing out the "workspaces básico" slice: new `lib/useWorkspaces.ts` SWR hook wraps `GET /api/v1/workspaces`, deriving `activeWorkspace` from the session's `active_workspace_id`; every mutation that affects workspace state (activate, create, member changes) revalidates the same shared `WORKSPACES_KEY` so every consumer picks up the change without its own plumbing. `components/layout/Header.tsx` gained a `WorkspaceSwitcher` dropdown (active workspace name as trigger, personal one labeled "Pessoal", a "+ Criar workspace" entry opening `CreateWorkspaceModal.tsx`); selecting a different workspace calls `.../activate` then does a blanket `mutate(() => true)` — a global SWR revalidation rather than a targeted key list, since the switcher lives in the header and doesn't know which page is currently mounted. New "Equipe" tab in `/settings` (`components/workspaces/TeamSettingsForm.tsx`, same insertion pattern as Phase 8's "IA" tab): shows an explanatory message + "Criar workspace" button for a personal workspace, otherwise the member list with an owner-only invite form, per-member role dropdown (disabled for the last remaining owner, mirroring the backend guardrail), and remove/leave buttons. `DocumentCard.tsx` shows "Enviado por {nome}" when the active workspace's `member_count` (new field on `WorkspaceWithRole`, alongside `ai_configured`) is greater than 1. Manual verification (two real accounts, one invited as editor, one as viewer) surfaced that the dashboard's upload zone and every document/share-link edit affordance rendered unconditionally regardless of role — the backend already 403'd a viewer's attempt, but the button was still there to click. Threaded a `canEdit` prop (`activeWorkspace.role !== "viewer"`) through `DocumentList`/`DocumentCard` (hides the upload zone and per-document delete button), `DocumentDetailView`/`DocumentMeta` (hides "Editar"/"Excluir documento"), and `ShareLinkList`/`ShareLinkCard` (hides "Criar novo link", "Editar", "Revogar", "Duplicar" — "Copiar link" stays visible, it's read-only) so a viewer's UI actually matches what the API allows.
- Phase 9 (Team Workspaces, "workspaces básico" slice) — US-32, AI features resolve the workspace creator's key, not the acting user's or the uploader's: a shared workspace has exactly one AI identity — its creator — regardless of who uploaded a given document or who's currently logged in and viewing it. New `models/workspace.ts#getCreatorIdForDocument()` replaces every ad-hoc key lookup across the four AI call sites (`chat.ts`, `analyticsInsights.ts`, `followupEmail.ts`, `summarizer.ts`), all of which previously resolved the key from either `document.getOwnerId()` (now deleted, see US-31) or the acting request's own `userId` — the latter was a real bug: a non-creator member viewing analytics insights on a shared document would silently fall back to _their own_ key instead of the workspace's, or see the feature vanish/error depending on whether they'd configured one themselves. The public share response's `ai_chat_available` field (`models/shareLink.ts#fetchAndValidateTokenRow`) now joins through `workspaces`/`created_by` instead of the document's direct `user_id` for the same reason.
- Phase 9 (Team Workspaces, "workspaces básico" slice) — US-31, documents & share links become workspace-scoped instead of owner-scoped: every operation now resolves the relevant `workspace_id` and calls `models/workspace.ts#requireRole()` (built in US-29) rather than comparing against the uploader's `user_id` directly — so any member with sufficient role (`editor` to upload/edit/delete, `viewer` to read) can act on a document regardless of which teammate originally uploaded it. Migration `028-set-workspace-id-not-null-on-documents.sql` finally applies the `NOT NULL` constraint deferred since US-28, landing in the same PR as `models/document.ts#create()` populating it on every insert. Deliberate behavior change: a user with no membership in a document's workspace now gets `404` instead of the old `403` — existence and permission are both gated through workspace membership now, so a non-member can't distinguish "doesn't exist" from "not yours" (13 existing test assertions updated to match, all anticipated by the design). `models/document.ts#getOwnerId()` is removed entirely, replaced by the workspace-creator lookup used across AI features (see US-32 below).
- Phase 9 (Team Workspaces, "workspaces básico" slice) — US-30, member invitation & role management: `GET`/`POST /api/v1/workspaces/[id]/members` (list every member with their role, invite by email — existing accounts only, no invite-token/accept flow) and `PATCH`/`DELETE /api/v1/workspaces/[id]/members/[userId]` (change role, remove a member, or leave — `DELETE` on one's own `userId` doubles as "leave workspace" and is allowed at any role, unlike removing someone else which requires `owner`). Invite returns a specific `404` ("Nenhuma conta encontrada com esse email...") when no account matches, distinct from a generic not-found, since this is lead-invitation not a lookup; `409` if the email is already a member. New `ConflictError` (409) added to `infra/errors.ts`/`infra/controller.ts` — the codebase's error classes didn't have one yet. Both role-change and removal share a last-owner guard (`models/workspace.ts#assertNotLastOwner`): a workspace's sole `owner` can't be demoted or removed/leave, since that would strand the workspace with no one able to manage it — checked only when the change would actually result in zero owners, so a no-op "owner → owner" reassignment is never blocked even for the only owner. Every membership mutation 403s on the personal workspace, same as US-29's workspace-level mutations.
- Phase 9 (Team Workspaces, "workspaces básico" slice) — US-29, workspace CRUD + switching: `POST`/`GET /api/v1/workspaces` (create a team workspace — the requester becomes its `owner`; list every workspace the requester belongs to, personal one first, each with the requester's own `role`), `PATCH`/`DELETE /api/v1/workspaces/[id]` (rename/soft-delete, `owner`-only, `403` on the personal workspace since it can't be renamed or deleted), and `POST /api/v1/workspaces/[id]/activate` (switches `users.active_workspace_id`, `404` if the requester isn't a member — same response whether the workspace doesn't exist, is deleted, or belongs to someone else, so as not to leak which workspaces exist). New `models/workspace.ts#requireRole()` is the central authorization check every later workspace-scoped story (US-30/US-31) builds on: role hierarchy `owner` > `editor` > `viewer`, `NotFoundError` for a missing/deleted/non-member workspace, `ForbiddenError` for an insufficient role. `GET /api/v1/sessions` now also returns `active_workspace_id`, so the frontend knows the active workspace on every page load without an extra request. No frontend yet — API only, the switcher UI lands in US-33.
- Phase 9 (Team Workspaces, "workspaces básico" slice) — US-28, workspace data model: every user now belongs to an automatically-created personal workspace (`is_personal = true`) rather than owning documents directly, laying the foundation for shared team workspaces in the stories that follow (US-29 through US-33). New `workspaces` (migration `024`) and `workspace_members` (migration `025`, role `owner`/`editor`/`viewer`) tables; `documents.workspace_id` (migration `026`) backfills every existing document into its uploader's new personal workspace, and `users.active_workspace_id` (migration `027`) backfills to the same. `models/user.ts#create()` now creates the personal workspace + owner membership + active workspace pointer atomically with the user row itself, on a dedicated transaction client — a user with no personal workspace would be a broken state. `documents.workspace_id` is deliberately left nullable for now (not yet populated on new uploads, and not yet used for any authorization decision) — US-31 applies `NOT NULL` once `models/document.ts#create()` actually populates it; getting this sequencing wrong during development briefly 503'd every document upload (`null value in column "workspace_id" violates not-null constraint`), caught via manual verification before landing.
- Phase 8 (AI Features) — closes out the phase with all six planned stories, sharing one Anthropic client wrapper (`infra/ai.ts`, `claude-haiku-4-5-20251001`) built around a bring-your-own-key model rather than a single platform-wide credential: every AI feature runs against the specific document owner's own Anthropic key (`models/user.ts#getAiApiKey()`), which each user pastes into a new "IA" section on `/settings` (`components/settings/AiSettingsForm.tsx`) and never sees again afterward — the API only ever returns `{ configured: boolean }`, never the key itself. Stored encrypted at rest (`infra/encryption.ts`, AES-256-GCM via a new `ENCRYPTION_KEY` env var — a real reversible credential, unlike `PEPPER`'s one-way password hashing) in a new `users.ai_api_key_encrypted` column (migration `023`). Features degrade the same way as before, just resolved per-owner instead of from a global env var: fire-and-forget ones (summarization, insights) silently no-op when the owner hasn't configured a key; synchronous ones (chat, follow-up email) throw a `ServiceError` (503) with a specific "peça para o dono do documento configurar sua chave" message. A shared `ai_usage_log` table (migration `020`) backs a generic, DB-based rate limiter (`models/aiUsage.ts`) for every cost-incurring action — unlike the existing IP-based `infra/rate-limit.ts` (production-only, used for the public chat endpoint), this one works in every environment and is scoped per-user or per-document depending on the feature.
  - **Auto-summarization on upload**: migration `019-add-ai-summary-to-documents.sql` adds `documents.ai_summary`/`ai_summary_generated_at`. `models/summarizer.ts` extracts PDF text (`models/textExtraction.ts`, PDF-only for this pass — DOCX/PPTX extraction is a documented future addition) and asks Claude for a 2-5 sentence summary; triggered fire-and-forget right after upload (same pattern as the existing new-viewer notification email — not awaited, errors swallowed, response unaffected), reusing the already-in-memory upload buffer instead of re-fetching from storage.
  - **Summary endpoint**: `GET`/`POST /api/v1/documents/[id]/summary` — GET returns the current summary; POST regenerates synchronously (a single Haiku call finishes well within a normal request), rate-limited to 3/hour per user.
  - **Analytics insights**: `GET /api/v1/documents/[id]/analytics/insights` (`models/analyticsInsights.ts`) asks Claude for a 3-5 sentence natural-language summary of a document's analytics, cached on the `documents` row (migration `021`) and only regenerated when `total_views`/`last_viewed_at` change since the last generation — rate-limited to 10 regenerations/day per document (cache hits don't count). Returns a friendly "Nenhuma visualização ainda..." message with no views yet, and quietly falls back to the existing cache if the AI client isn't available.
  - **Drop-off suggestions**: the same insights endpoint asks Claude for up to 3 actionable suggestions (`drop_off` or `engagement` type) when there are ≥5 views, based on real per-page data rather than the `avg_pages_viewed` estimate the original spec assumed — `models/linkView.ts#getPageBreakdownByDocumentId()` aggregates `link_view_pages` across every link of the document (they all point at the same file, so combining them is meaningful, unlike combining across different documents), an improvement made possible by Phase 7's per-page tracking landing after this story was originally scoped.
  - **AI-drafted follow-up email suggestion**: new story, spec written this session (`user-stories/phase-7-ai/US-27-followup-email-suggestion.md`) since it depends on Phase 7's engagement score. `POST /api/v1/documents/[id]/links/[linkId]/followup-email` (`models/followupEmail.ts`) drafts a subject+body referencing at least one concrete engagement signal (page reached, download, visit count) for a specific viewer, identified by the `viewer_fingerprint` now exposed on `ViewerEngagement` (Phase 7's per-viewer list). Ownership/link/viewer-existence checks and the 20/hour rate limit all run _before_ the AI-availability check, so a request from someone without access gets the correct 403/404, not a misleading 503. Never persisted or auto-sent — the owner copies and sends it themselves. New "Sugerir e-mail" button on each row of `ViewerEngagementList.tsx`.
  - **Viewer chat (RAG)**: `POST /api/v1/share/[token]/chat` — public, gated exactly like the sibling share routes (password/email/NDA via `shareLink.getByToken`) before any AI work happens, then streams a Claude response via Server-Sent Events. `models/documentChunks.ts` lazily extracts and stores one chunk per PDF page (migration `022-create-document-chunks.sql`) on first use — most links are never chatted with, so this avoids doing it unconditionally at upload time — and retrieves the most relevant pages for a question via plain keyword-overlap scoring, no vector DB/embeddings pipeline for this MVP. New floating "Perguntar sobre este documento" toggle and `components/viewer/ChatPanel.tsx` on the PDF viewer, reading the SSE stream via `response.body.getReader()` (not `EventSource`, since this needs a POST body and custom auth headers).
  - **Hide AI features when unconfigured**, following up on the BYOK switch above: rather than showing an AI feature and having it fail (fire-and-forget silent no-op, or a 503 on a synchronous action), the four AI-feature entry points now don't render at all unless the relevant key is present. `lib/useAiKeyConfigured.ts` (new) is a small SWR hook that reads the logged-in user's own status from the same `/api/v1/users/[username]/ai-key` cache key `AiSettingsForm.tsx` already uses, so saving or removing a key in Settings instantly reflects everywhere else it's read — no separate global-state layer needed. Applied to `SummaryCard.tsx` (hides the whole card, even if a summary was generated before the key was removed) and `InsightCard.tsx` (also makes its SWR fetch itself conditional, avoiding a wasted network call) on the document/analytics owner-facing pages, and to the "Sugerir e-mail" button in `ViewerEngagementList.tsx`. The public viewer's floating chat toggle needed the _document owner's_ configuration, not the anonymous viewer's, so it's resolved server-side instead: `models/shareLink.ts#getByToken()` now returns a new `ai_chat_available` boolean (`u.ai_api_key_encrypted IS NOT NULL`, joined off the document's owner) alongside the rest of the public share response, and `ViewerPage.tsx` gates the toggle button on it.
- Phase 7 (Engagement, Trust & Growth) — Composite engagement score per viewer, surfaced as a new per-viewer list in the per-link analytics drawer, closing out Phase 7. Migration `018-add-downloaded-to-link-views.sql` adds `link_views.downloaded`, set (never unset) via `downloaded = downloaded OR $x` in `models/linkView.ts#upsertViewWithDedup` — tracked client-side by `ViewerPage.tsx`'s `hasDownloadedRef`, sent on the existing `beforeunload` beacon rather than firing an extra request per download. `models/linkView.ts#getViewersByLinkId()` groups `link_views` rows by fingerprint (a returning visitor outside the 30-min dedup window gets a new row, aggregated back together here) and `computeEngagementScore()` blends four signals into one 0-100 number: time on page and % of pages viewed (30% each, since finishing the document matters most), return visits and download (20% each) — 2 minutes of reading or 3+ visits each earn full marks on their own axis. A document with no known page count (non-PDF files) simply contributes 0 on the pages axis rather than guessing. `GET /api/v1/documents/[id]/links/[linkId]/analytics` now takes the document's `page_count` and returns a `viewers` array sorted highest-score-first; rendered by the new `components/analytics/ViewerEngagementList.tsx` in `LinkAnalyticsDrawer.tsx`, which also picked up the same scrollable-dialog treatment as the share-link modals below, since the added list pushed its content past the viewport on smaller screens.
- Phase 7 (Engagement, Trust & Growth) — NDA/consent gate: migration `016-add-nda-text-to-share-links.sql` adds `share_links.nda_text`; migration `017-add-viewer-name-to-link-views.sql` adds `link_views.viewer_name`, captured alongside the existing `viewer_email`. When configured, a visitor must accept the term (submitting name + email) before viewing — enforced last in `models/shareLink.ts#fetchAndValidateTokenRow`'s gating pipeline, independent of the allow-list/require-email checks so a link can combine an NDA with an allow-list (both are checked, neither short-circuits the other). The NDA text itself isn't sensitive — it's meant to be read _before_ any credential is supplied — so it's served by a new permission-free endpoint, `GET /api/v1/share/[token]/nda` (`models/shareLink.ts#getNdaText()`), mirroring the existing `getPublicMetadata()` precedent from the OG-meta-tags feature: returns `null` (not an error) for a revoked/expired/deleted/nonexistent link or one with no NDA set. `components/viewer/NdaGate.tsx` (new) renders the term plus a name/email form; `ViewerPage.tsx` gains a parallel `nda-required` state, fetching the term text on 403 and resubmitting name/email like the existing password/email retry flow. Editable via a `Textarea` in both share-link modals, shown as a "Termo de NDA" badge on `ShareLinkCard.tsx`.
- Phase 7 (Engagement, Trust & Growth) — Custom branding per share link: migration `015-add-branding-fields-to-share-links.sql` adds `share_links.brand_accent_color` (hex, validated `#RRGGBB`) and `brand_welcome_message`. Applied only once a visitor reaches the "ready" (unlocked) viewer state — not on the gate screens — to avoid needing another permission-free peek endpoint, keeping the feature cheap to build now as scoped in `TODO.md`; it's a paid-tier hook in Phase 10. `ViewerPage.tsx` overrides the `--primary` CSS custom property inline on the ready-state wrapper (Tailwind's `bg-primary`/`text-primary` utilities resolve it at paint time, so no component changes are needed) and renders the welcome message as a banner above the document. Editable via a color input + `Textarea` in both share-link modals, shown as a "Personalizado" badge on `ShareLinkCard.tsx`.
- Phase 7 (Engagement, Trust & Growth) — "Duplicate settings" when creating a share link from an existing one: a new "Duplicar" button on `ShareLinkCard.tsx`, next to Editar/Revogar, opens `CreateShareLinkModal.tsx` pre-filled from the source link (label gets a "(cópia)" suffix, expiry/toggles/allow-list all carried over) — except the password, which can't be copied since only its bcrypt hash is ever available; the field shows a placeholder note instead and the owner sets a new one if needed. `CreateShareLinkModal` gained optional `prefill`/`open`/`onOpenChange` props so it can run in this externally-controlled "duplicate" mode without its own trigger button, alongside its existing self-contained "Criar novo link" mode; `ShareLinkList.tsx` lifts the one piece of state needed (which link, if any, is being duplicated) and remounts the modal via `key` per target link so `useState`'s prefill-seeding runs fresh each time.
- Phase 7 (Engagement, Trust & Growth) — Open Graph/Twitter meta tags on public share links, so pasting one into Slack/iMessage/Twitter renders a proper preview card (document title + description) instead of a bare URL. `app/view/[token]/page.tsx` converted from a client-only page to a Server Component wrapping the existing client `ViewerPage`, adding a `generateMetadata()` export. Backed by a new `models/shareLink.ts#getPublicMetadata()` — deliberately skips the password/require-email/allow-list checks (crawlers/link-unfurlers never have the credential a gated link needs) and returns only the document's own title/description, nothing sensitive; falls back to generic "Documento compartilhado" copy for a revoked/expired/deleted/nonexistent link so nothing about it leaks. The title still shows for password-protected links — the same trade-off Dropbox/Google Drive make in their own link previews.
- Phase 7 (Engagement, Trust & Growth) — Dynamic PDF watermark: migration `014-add-watermark-enabled-to-share-links.sql` adds `share_links.watermark_enabled` (default `false`). Like the allow-list, enabling it implies an email is required regardless of the `require_email` toggle — the watermark needs an identity to burn in. `components/viewer/PDFViewer.tsx#drawWatermark()` tiles the viewer's email + a load-time timestamp diagonally across the canvas immediately after `page.render()`, so it's baked into the rendered bitmap (survives a screenshot or browser print) rather than a removable DOM overlay — not tamper-proof against someone editing the client, and the downloadable file itself is untouched, both accepted trade-offs for a canvas-render-only deterrent. The watermark text is computed once in `ViewerPage.tsx` when the document first loads (a ref, not recomputed on zoom/page-change) and passed down as a prop. Editable via a `Switch` in both share-link modals, shown as a "Marca d'água" badge on `ShareLinkCard.tsx`.
- Phase 7 (Engagement, Trust & Growth) — Per-link email allow-list: migration `013-create-share-link-allowed-emails.sql` — `share_link_allowed_emails` table (case-insensitive unique index on `(share_link_id, LOWER(email))`), cascades on link deletion. `models/shareLink.ts#fetchAndValidateTokenRow` checks it after the password gate, and its presence implies an email is required even with `require_email` off — the owner shouldn't have to flip two toggles. Full-replace semantics on save (`replaceAllowedEmails()`): the edit form always sends its current textarea contents, not an add/remove diff. Editable via a `Textarea` (one email per line, parsed/deduped client-side by `lib/parseAllowedEmails.ts`) in both share-link modals, shown as a "Lista de emails (N)" badge on `ShareLinkCard.tsx`. Reuses the existing `email-required` viewer state and `EmailGate.tsx` from the "require email" feature — just a different rejection message ("Email não autorizado.") when the submitted address isn't on the list.
- Phase 7 (Engagement, Trust & Growth) — Per-link "require email" gate: migration `011-add-require-email-to-share-links.sql` adds `share_links.require_email` (default `false`); migration `012-add-viewer-email-to-link-views.sql` adds `link_views.viewer_email` so the captured address is stored on the view record. `models/shareLink.ts#fetchAndValidateTokenRow` checks it after the password gate (one thing to fix at a time if a link has both), via a new `X-Viewer-Email` header on `GET /api/v1/share/[token]` and `.../file`, validated with a plain `z.string().email()` check (`infra/schemas.ts#isValidEmail`) — no ownership/verification email loop, this is lead-capture, not authentication. `components/viewer/ViewerPage.tsx` gains a parallel `email-required` state and `EmailGate.tsx` (mirrors the existing `PasswordGate.tsx`), and resends whichever of password/email it already has on every retry so a link with both gates resolves progressively. Editable via a `Switch` in both share-link modals (same pattern as `notify_on_view`) and shown as a "Requer email" badge on `ShareLinkCard.tsx`. Bonus: the existing new-viewer notification email now names the viewer (`infra/mailer.ts`) when their email was captured — HTML-escaped, since it's attacker-controlled input from an anonymous endpoint landing in the owner's inbox.
- Superadmin role + `/superadmin/migrations` page: migration `010-add-is-superadmin-to-users.sql` adds `users.is_superadmin` (default `false`) — named "superadmin" rather than "admin" so it can't collide with a future account/workspace-level admin role. `infra/auth.ts#migrationsAuthMiddleware` now accepts either the existing `x-migrations-secret` header (unchanged, for scripts/CI) or a logged-in superadmin session, so a superadmin can view/run pending migrations from the browser without ever handling the raw secret. The "Superadmin" nav link (`components/layout/Header.tsx`) only renders for `is_superadmin` users, and `/superadmin/migrations` (Server Component, `components/superadmin/MigrationsPanel.tsx`) redirects everyone else to `/dashboard`. There's deliberately no API/UI to grant superadmin — promoting an account is a one-time manual `UPDATE users SET is_superadmin = true WHERE email = '...'`, documented in `CLAUDE.md`, so no one's email ends up hardcoded in a migration committed to this public repo.
- Phase 7 (Engagement, Trust & Growth) — Per-page analytics heatmap (DocSend's headline differentiator in this category):
  - Migration `009-create-link-view-pages.sql` — `link_view_pages` table, one row per `(link_view_id, page_number)`; repeat reports for the same view+page accumulate time via `ON CONFLICT ... DO UPDATE` instead of overwriting
  - `components/viewer/ViewerPage.tsx` tracks per-page dwell time client-side and includes it as `page_times` in the same `sendBeacon` payload already used for the aggregate `time_on_page`/`pages_viewed` on unload
  - `models/linkView.ts` persists `page_times` (in the same transaction as the existing dedup upsert) and exposes a new `page_breakdown` (avg time + view count per page) on `GET /api/v1/documents/[id]/links/[linkId]/analytics` — per-link only, since a page-by-page breakdown doesn't aggregate meaningfully across a document's different links
  - `components/analytics/PageHeatmapChart.tsx` — new bar chart (recharts, reusing the existing `--color-primary` token) rendered in `LinkAnalyticsDrawer.tsx`
- Phase 7 (Engagement, Trust & Growth) — Per-link toggle to mute view notifications: migration `008-add-notify-on-view-to-share-links.sql` adds `notify_on_view BOOLEAN NOT NULL DEFAULT TRUE`; exposed on create/update via `ShareLinkCreateInput`/`ShareLinkUpdateInput`, checked in the view-recording route before firing the notification email, and editable via a `Switch` in both `CreateShareLinkModal.tsx` and `EditShareLinkModal.tsx` (same pattern as the existing `is_active` toggle).
- Phase 7 (Engagement, Trust & Growth) — Email notification when a share link gets a new viewer:
  - `infra/mailer.ts` — Resend-backed mailer, following the same environment-gated no-op pattern as `infra/storage.ts` (no-op in `NODE_ENV=test` and when `RESEND_API_KEY` is unset, so this never requires a real API key locally or in CI)
  - `models/linkView.ts#recordView()` now distinguishes a genuinely new viewer (no prior row for that fingerprint on this link, at any time) from a returning one outside the existing 30-minute dedup window, and returns `is_new_viewer` on the `POST /api/v1/share/[token]/view` response
  - `models/shareLink.ts#getNotificationInfo()` — looks up the document owner's email, document title, and link label for a given share link
  - The view-recording route fires the notification fire-and-forget, so a mailer failure (missing key, Resend outage, deleted owner) never affects the response to the anonymous viewer; the analytics-page link in the email is built from the request's own `Host`/`X-Forwarded-Proto` headers rather than a new env var, so it works unmodified across local/preview/production
- Phase 6 — Frontend (all 6 blocks complete):
  - Block 1 — Foundation: `GET /api/v1/sessions` (thin authenticated handler backing the frontend auth context), `models/user.ts#findOneById()`, App Router foundation (Tailwind CSS v4 + shadcn/ui on a Radix base), `context/AuthContext.tsx` + `lib/fetcher.ts` + `app/providers.tsx` (SWR-backed `useAuth()`), `lib/auth-server.ts#getServerUser()` for server-side auth gating with no client-side flicker, shared `Header`/`Footer` + landing page (`app/page.tsx`); `pages/index.tsx`/`pages/status/index.tsx` migrated to `app/page.tsx`/`app/status/page.tsx` (`pages/api/v1/*` untouched)
  - Block 2 (US-08) — `app/register/page.tsx`/`app/login/page.tsx` (Server Component gates, redirect to `/dashboard` if already authenticated) with `components/forms/RegisterForm.tsx`/`LoginForm.tsx`
  - Block 3 (US-09) — `app/dashboard/page.tsx`; `components/documents/UploadZone.tsx` (drag-and-drop + XHR upload with progress), `DocumentCard.tsx`, `DocumentList.tsx` (paginated, SWR); `lib/formatters.ts`
  - Block 4 (US-10) — `app/documents/[id]/page.tsx`; `components/documents/DocumentDetailView.tsx`/`EditDocumentForm.tsx`/`DocumentMeta.tsx`; `components/share-links/CreateShareLinkModal.tsx`/`EditShareLinkModal.tsx`/`ShareLinkCard.tsx`/`ShareLinkList.tsx`
  - Block 5 (US-11) — `app/view/[token]/page.tsx` (public, client-rendered); `components/viewer/ViewerPage.tsx` (fetches share metadata + file bytes with the `X-Share-Password` header, records views via `POST .../view` on load and `navigator.sendBeacon` on unload), `PDFViewer.tsx` (canvas rendering via pdfjs-dist), `ViewerControls.tsx`, `PasswordGate.tsx`; `lib/fingerprint.ts`
    - New backend support: `infra/storage.ts#getFile()`, `models/shareLink.ts#getFileByToken()` (shares the existing token/password/expiry validation helper with `getByToken()` but additionally returns `storage_key`), `GET /api/v1/share/[token]/file` — public, rate-limited file-proxy route so the viewer can fetch file bytes with a password header (a direct `<img>`/browser navigation can't attach custom headers)
  - Block 6 (US-12, US-13) — `app/documents/[id]/analytics/page.tsx`; `components/analytics/AnalyticsView.tsx`/`StatCard.tsx`/`ViewsChart.tsx` (recharts)/`TopLinksTable.tsx`/`LinkAnalyticsDrawer.tsx`; `app/settings/page.tsx`; `components/settings/ProfileForm.tsx`/`DangerZone.tsx` (logout, soft-delete account)
  - Each corresponding user story in `user-stories/phase-6-frontend/` has an alignment note reconciling its original Pages-Router-era spec with the App Router / shadcn decisions made in Block 1
- Phase 5 — Analytics & Tracking:
  - Migration `007-create-link-views.sql` — `link_views` table, cascades on share link deletion
  - `POST /api/v1/share/[token]/view` — public view-recording endpoint; extracts `ip_address`/`user_agent` from headers, does not require the link password (already gated by the public GET)
  - `models/shareLink.ts#validateToken()` — existence/active/expiry check without the password gate, so view recording doesn't need the viewer to re-supply the password
  - `models/linkView.ts#recordView()` — deduplicates repeat views from the same `viewer_fingerprint` within a 30-minute window (updates `time_on_page`/`pages_viewed` on the existing row instead of inserting a new one), using a transaction + `SELECT ... FOR UPDATE` on a dedicated client to avoid a race between the check and the write
  - `GET /api/v1/documents/[id]/links/[linkId]/analytics` and `GET /api/v1/documents/[id]/analytics` — aggregated view stats (total/unique/avg time/avg pages/first-last viewed) plus a 30-day zero-filled `views_by_day` series; the per-document endpoint also returns `top_links` (top 5 by views)
  - All aggregate SQL casts `COUNT`/`AVG` results to `::int`/`::float` — `pg` returns `bigint`/`numeric` as strings by default, which would otherwise leak into the API response as strings instead of numbers
- Phase 4 — Share Links:
  - Migration `006-create-share-links.sql` — `share_links` table (plaintext UUID `token`, bcrypt `password_hash`, `expires_at`, `allow_download`, `is_active` for soft-revoke)
  - `models/shareLink.ts` — `create`, `findAllByDocumentId`, `findOneById`, `updateById`, `revokeById`, `getByToken` (validates active/expiry/password/document-deleted, in that order)
  - `POST`/`GET /api/v1/documents/[id]/links` — create and list share links, ownership enforced via `models/document.ts#findOneById`
  - `PATCH`/`DELETE /api/v1/documents/[id]/links/[linkId]` — update link config (label/password/expiry/allow_download/is_active, `null` clears password or expiry) and soft-revoke
  - `GET /api/v1/share/[token]` — public, unauthenticated endpoint; rate-limited (20 req/min); accepts the optional link password only via `X-Share-Password` header, never a query param
  - `infra/schemas.ts` — `shareLinkCreateSchema`/`shareLinkUpdateSchema`, `expires_at` must be a future ISO datetime
  - Share link tokens are intentionally **not** hashed at rest (unlike session tokens) — see the `TODO.md` Phase 4 note for why, and what was hardened instead (header-only password, rate limiting, narrow public response shape)
- Phase 3 — Documents Core:
  - Migration `005-create-documents.sql` — `documents` table (soft-delete, `user_id` FK, indexed)
  - `infra/storage.ts` — S3-compatible storage adapter (`@aws-sdk/client-s3`); MinIO locally via `infra/compose.yaml`, AWS S3/Cloudflare R2 in production; no-op in `NODE_ENV=test`
  - `POST /api/v1/documents` — multipart upload (`formidable`), Zod-validated title/description, MIME allowlist (PDF/DOCX/PPTX), size limit via `MAX_FILE_SIZE_MB`, PDF page-count extraction via `pdf-parse`
  - `GET /api/v1/documents` — paginated list of the authenticated user's documents (`page`/`per_page` query params)
  - `GET`/`PATCH`/`DELETE /api/v1/documents/[id]` — ownership-checked read, partial update, and soft-delete (with storage cleanup) of a single document
  - `models/document.ts` — `create`, `findAllByUserId`, `findOneById`, `updateById`, `deleteById`; ownership enforcement returns `ForbiddenError` (403) vs `NotFoundError` (404)
  - `tests/fixtures/sample.pdf` and `orchestrator.uploadDocument()` test helper
  - `services:up`/`services:down`/`services:stop` now pass `--env-file .env.development` to `docker compose` so the new `storage` (MinIO) service's env var substitution resolves correctly
- Pre-Phase-3 hardening sprint:
  - `infra/auth.ts#migrationsAuthMiddleware` — `GET`/`POST /api/v1/migrations` now require a matching `x-migrations-secret` header (`MIGRATIONS_SECRET` env var); previously unauthenticated and able to run migrations against the live database
  - `models/session.ts` — session tokens are now stored as a SHA-256 hash; the raw bearer token is never persisted
  - `models/user.ts` — Postgres unique-violation (`23505`) on insert/update is now caught and mapped to `ValidationError`, closing the check-then-insert race in `validateUniqueEmail`/`validateUniqueUserName`
  - `typecheck` job (`npx tsc --noEmit`) added to `.github/workflows/linting.yaml`; required adding `baseUrl: "."` to `tsconfig.json` so `tsc` resolves the root-style imports (`tests/orchestrator`, `models/session`, …) some test files already used, matching jest's `moduleDirectories` resolution
- Rate limiting middleware (`infra/rate-limit.ts`) — in-memory sliding window; 5 req/min on `POST /api/v1/sessions`, 10 req/min on `POST /api/v1/users`; returns `TooManyRequestsError` (429)
- `TooManyRequestsError` (429) error class in `infra/errors.ts`
- Zod input validation (`infra/schemas.ts`) on `POST /api/v1/users`, `PATCH /api/v1/users/[username]`, `POST /api/v1/sessions` — invalid input returns 400 `ValidationError`
- `session.deleteByUserId(userId)` — invalidates all sessions for a user; called on account deletion
- `DELETE /api/v1/users/[username]` — soft-delete own account (`deleted_at` column), returns 204; all sessions invalidated, deleted users are no longer findable
- `ForbiddenError` (403) error class in `infra/errors.ts`
- Authorization guard on `PATCH /api/v1/users/[username]` — returns 403 if authenticated user tries to update another user's profile
- Authorization guard on `DELETE /api/v1/users/[username]` — returns 403 if authenticated user tries to delete another user's account
- Migration `004-soft-delete-users.sql` — adds `deleted_at TIMESTAMPTZ` column to `users` table
- `user.deleteByUsername(username)` — sets `deleted_at` to now
- Database connection pool (`Pool` from pg) for all regular queries — `getNewClient()` still returns a dedicated `Client` for the migrator
- `DELETE /api/v1/sessions` — logout endpoint, deletes session from DB and clears `session_id` cookie, requires valid session
- Authentication middleware (`infra/auth.ts`) that validates `session_id` cookie on protected routes
- `session.findOneByToken(token)` — look up a session by its token
- `session.deleteByToken(token)` — delete a session (used on expiration cleanup)
- `GET /api/v1/users/[username]` and `PATCH /api/v1/users/[username]` now require a valid session
- Integration tests for all authentication middleware scenarios (no cookie, invalid token, expired session, valid session)
- Test helpers `orchestrator.createExpiredSession(userId)` and `orchestrator.sessionExists(token)`

### Fixed

- Renamed `UnathorizedError` → `UnauthorizedError` (typo was part of the API error contract's `name` field)
- Session cookies (`session_id`) now set `SameSite=Lax` explicitly instead of relying on the browser default
- Removed `console.log` calls from `models/migrator.ts` (violated the "no console.log in production code" convention)

---

## [0.2.0] — 2026-03-28

### Added

- `POST /api/v1/sessions` — login endpoint that creates a session and sets an HTTP-only `session_id` cookie (30-day expiration)
- `models/session.ts` with `create(userId)` and `EXPIRATION_IN_MILLISECONDS` constant
- `models/authentication.ts` — validates email/password credentials via `getAuthentication()`
- Migration `003-create-sessions.sql` — `sessions` table with UUID primary key, unique token, `user_id`, and `expires_at`
- Integration tests for the login flow (valid credentials, wrong password, unknown email)

### Changed

- Migrated database migration runner from custom implementation to `postgres-migrations` library
- Adjusted migration SQL files for compatibility with the new runner

---

## [0.1.0] — 2026-03-20

### Added

- Initial project setup with Next.js 14, TypeScript, and PostgreSQL
- Clean architecture layout: `pages/` (delivery), `models/` (domain), `infra/` (technical)
- `POST /api/v1/users` — user registration with unique username and email validation
- `GET /api/v1/users/[username]` — fetch user profile by username
- `PATCH /api/v1/users/[username]` — update user profile (username, email, password)
- `GET /api/v1/status` — health check endpoint with database version and connection info
- `GET /api/v1/migrations` — list pending migrations
- `POST /api/v1/migrations` — run pending migrations
- `models/user.ts` — user CRUD with case-insensitive lookups
- `models/password.ts` — bcryptjs hashing with pepper support
- `models/migrator.ts` — migration runner using `postgres-migrations`
- Migration `001-create-users.sql` — `users` table with UUID primary key
- Migration `002-update-users.sql` — UTC timestamps and adjusted password column length
- Custom error classes: `ValidationError` (400), `NotFoundError` (404), `UnathorizedError` (401), `InternalServerError` (500), `ServiceError` (503), `MethodNotAllowedError` (405)
- `infra/controller.ts` — global Next.js error handler middleware
- `infra/database.ts` — PostgreSQL client with environment-based configuration
- Docker Compose setup with PostgreSQL 16
- Jest integration test suite with `tests/orchestrator.ts` helpers
- ESLint, Prettier, Husky pre-commit hooks, and Commitlint
- GitHub Actions CI pipeline
